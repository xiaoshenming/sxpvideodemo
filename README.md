# sxpvideodemo

数学派 sxpagents 的 Manim 视频 demo 仓库。

当前任务主题：勾股定理。

## 结构

- `视频.mp4`：永远只保留最新、当前质量最好的成片。
- `最新.py`：与当前视频对应的 Manim 源码。
- `最新问题.md`：与当前视频对应的问题、评分、修复记录。
- `history/`：历史版本源码、问题记录和元数据归档。
- `runs/`：每次自动生成/渲染的运行记录。
- `assets/`：必要的小型素材。

> 禁止提交 API key、.env、临时渲染缓存、大型无关素材。
