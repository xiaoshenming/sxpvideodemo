# 勾股定理 demo 问题记录

运行时间：2026-04-29 17:09:07 +08:00
来源：sxpagents `outputs/demo-pythagoras-20260429-170907`
主题：勾股定理：用几何面积重排证明 a^2+b^2=c^2，要求画面清晰、节奏稳定、适合初中生理解

## 本次产物

- 当前视频：`视频.mp4`
- 当前源码：`最新.py`
- 历史归档：`history/20260429-170907.py`
- 历史问题：`history/20260429-170907-问题.md`

## 质量结果

- 生成引擎：deterministic template fallback
- 静态 validator：通过
- quality score：90.0
- Manim 渲染：通过，使用 `shuxuepai` conda 环境的 Manim 0.18.1
- 拼接：通过，使用 ffmpeg concat 三个 Scene

## 发现的问题

1. LLM 生成第一次失败：`LLM HTTP response was not valid JSON`。
   - 说明 runtime LLM adapter 还需要更强的非 JSON 响应截断/清洗/重试机制。
2. sxpagents conda 环境缺少 `manim` 命令。
   - 本次临时改用 `conda run -n shuxuepai manim` 渲染。
   - 后续应让 sxpagents 的 render smoke 自动发现可用 Manim 环境。
3. 初版脚本使用 `FadeOut(VGroup(*self.mobjects))` 清场。
   - 在 Manim 0.18.1 中 `self.mobjects` 可能包含非 VMobject 的基础 `Mobject`，导致 `VGroup` 报错。
   - 本次临时把 demo 源码改为 `FadeOut(Group(*self.mobjects))` 后渲染通过。
   - 后续应把这个问题固化为 generator 输出规则或 validator：清场应使用 `Group(*self.mobjects)` / 安全 helper，不要用 `VGroup(*self.mobjects)` 包全部 mobjects。

## 下一步迭代建议

- Codex worker-1：修复 LLM JSON 响应解析/重试/metadata 记录。
- Codex worker-2：修复生成器清场 helper，避免 `VGroup(*self.mobjects)`。
- Codex worker-3：补 validator，发现危险清场写法并给 repair hint。
- Codex worker-4：把本次 demo 记录纳入样本/质量对比数据集。
