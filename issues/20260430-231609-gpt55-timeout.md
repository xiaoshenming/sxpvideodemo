# gpt-5.5 业务生成超时记录

- generated_at: 20260430-231609
- publish_decision: not_published
- reason: OpenAI-compatible gpt-5.5 request timed out; deterministic fallback disabled by model-boundary gate
- command: `PYTHONPATH=src SXPA_LLM_TIMEOUT_SECONDS=240 python -m sxpagents.cli generate "热力学第二定律：从冰块融化到信息熵" --output outputs/worker5-entropy-20260430-231609 --max-scenes 10 --llm`
- error: `LLM network timeout while waiting for response (attempts=2, timeout_seconds=240, endpoint=https://aigc.x-see.cn/v1/chat/completions)`

## 边界结论

- 业务生成要求 gpt-5.5 时，不能把 deterministic fallback 成品当作 gpt-5.5 生成结果发布。
- 本轮没有更新根目录 `视频.mp4`、`最新.py`、`最新问题.md`。
- 当前 best 已做客观检查：`854x480`、`117.33s`、`1760` 帧，未检测到后半段长黑屏。

## 反馈给生成链路

- worker-1/runtime：继续检查所有业务 LLM 入口是否复用 `load_llm_config()`，并考虑把长任务的超时/重试参数纳入可观测 metadata，但不要记录 key。
- worker-2/graph：graph/runner 需要把 `LLMResponseError` 作为明确失败状态返回，不要自动降级成 deterministic 成品。
- worker-4/history：可把 `llm_timeout`、`fallback_disabled`、`video_objective_check_failed` 纳入 history 问题码统计。
